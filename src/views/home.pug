extends layouts/main
include mixins/videoBlock

block content
    form(action="/search", method="GET")
            input(type="text", placeholder="search by term...", name="keyword")
            input(type="submit", value="Search").inputBtn
    div
        each video in videos
            +videoBlock(video)